export const TAX_TYPES = {
  INTRA_STATE: 'CGST_SGST', // CGST + SGST (intra-state)
  INTER_STATE: 'IGST'       // IGST (inter-state)
};

export const CALCULATION_MODES = {
  EXCLUSIVE: 'EXCLUSIVE', // Add GST
  INCLUSIVE: 'INCLUSIVE'  // Remove GST
};
